name: multi_package_sample
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.15.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  home:
    path: ../features/home/
  login:
    path: ../features/login/
  splash:
    path: ../features/splash/

  common_dependencies:
    path: ../infrastructure/common_dependencies/
  core:
    path: ../infrastructure/core/

  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
